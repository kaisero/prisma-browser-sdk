# coding: utf-8

"""
    Prisma Browser Management Console Public API

    This Open API spec file represents the APIs available for Palo Alto Networks Prisma Browser Management APIs. These APIs are available for all Prisma Browser tenants to scale and automate their processes related to the browser. All of the functionality exposed by these APIs can also be performed using the Prisma Browser User Interface. These APIs use the common SASE authentication mechanism and base URL. See the Prisma SASE API Get Started guide for more information. © 2026 Palo Alto Networks, Inc. Palo Alto Networks is a registered trademark of Palo Alto Networks. A list of our trademarks can be found at https://www.paloaltonetworks.com/company/trademarks.html. All other marks mentioned herein may be trademarks of their respective companies.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from prisma_browser.models.position_item_rule import PositionItemRule
from prisma_browser.models.position_item_section import PositionItemSection
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

POSITIONITEM_ONE_OF_SCHEMAS = ["PositionItemRule", "PositionItemSection"]

class PositionItem(BaseModel):
    """
    A section or rule in the desired position order
    """
    # data type: PositionItemSection
    oneof_schema_1_validator: Optional[PositionItemSection] = None
    # data type: PositionItemRule
    oneof_schema_2_validator: Optional[PositionItemRule] = None
    actual_instance: Optional[Union[PositionItemRule, PositionItemSection]] = None
    one_of_schemas: Set[str] = { "PositionItemRule", "PositionItemSection" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PositionItem.model_construct()
        error_messages = []
        match = 0
        # validate data type: PositionItemSection
        if not isinstance(v, PositionItemSection):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PositionItemSection`")
        else:
            match += 1
        # validate data type: PositionItemRule
        if not isinstance(v, PositionItemRule):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PositionItemRule`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PositionItem with oneOf schemas: PositionItemRule, PositionItemSection. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PositionItem with oneOf schemas: PositionItemRule, PositionItemSection. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # use oneOf discriminator to lookup the data type
        _data_type = json.loads(json_str).get("type")
        if not _data_type:
            raise ValueError("Failed to lookup data type from the field `type` in the input.")

        # check if data type is `PositionItemRule`
        if _data_type == "Rule":
            instance.actual_instance = PositionItemRule.from_json(json_str)
            return instance

        # check if data type is `PositionItemSection`
        if _data_type == "Section":
            instance.actual_instance = PositionItemSection.from_json(json_str)
            return instance

        # deserialize data into PositionItemSection
        try:
            instance.actual_instance = PositionItemSection.from_json(json_str)
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into PositionItemRule
        try:
            instance.actual_instance = PositionItemRule.from_json(json_str)
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PositionItem with oneOf schemas: PositionItemRule, PositionItemSection. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PositionItem with oneOf schemas: PositionItemRule, PositionItemSection. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], PositionItemRule, PositionItemSection]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


